[build]
target = "x86_64-unknown-linux-none"

# TODO: implement runtime ELF relocations and restore PIE.
# The linux-none target defaults to PIE, but neko currently has no
# runtime relocator, so position-dependent code is required.
# rustflags = ["-C", "relocation-model=static"]

[unstable]
build-std = ["core"]